---
phase: 02-artist-identity-artwork-migration
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified:
  - deploy/verify-artist-artwork-migration.sh
autonomous: false
requirements: [ARTIST-01, ARTIST-02, ARTIST-03, ART-01, ART-02, ART-03]
user_setup: []

must_haves:
  truths:
    - "On the real Pi, Library -> Artists shows a single 'Herbert von Karajan' row, not the raw joined 'Herbert von Karajan  Wiener Philharmoniker' string"
    - "On the real Pi, Library -> Artists shows a single 'Luciano Pavarotti' row collapsing every one of the 15 label-credit variants"
    - "The artist list contains no blank row on the LCD, where MPD's empty list-artist value previously produced one"
    - "Artist artwork that rendered before the collapse still renders after it for the same real artists — no broken-image placeholders"
    - "The count of albums with no artwork link drops from the live baseline toward 0 (or stays equal if zero confident visual matches exist) without any new network call to Fanart.tv, Deezer, or MusicBrainz"
    - "Re-running the migration (a second restart / rebuild) changes nothing and deletes no files under the artwork cache directory"
  artifacts:
    - path: deploy/verify-artist-artwork-migration.sh
      provides: "On-Pi PASS/FAIL gate script for this phase's 5 ROADMAP success criteria, matching the deploy/verify-data-integrity.sh idiom"
    - path: ".planning/phases/02-artist-identity-artwork-migration/evidence/"
      provides: "Before/after count snapshots + artist-list JSON captures (D-11 evidence, not assertions)"
  key_links:
    - from: "systemctl restart stellar-backend"
      to: "internal/transport/socketio/server.go InitializeCache -> cache.MigrateArtistArtwork"
      via: "the artist-artwork rekey runs automatically on this boot, before any cache rebuild wipes the pre-collapse artist names it needs to read"
      pattern: "MigrateArtistArtwork"
    - from: "python3 deploy/rebuild-cache.py"
      to: "internal/infra/cache/builder.go buildArtists (collapse-wired by Plan 02-02)"
      via: "library:cache:rebuild Socket.IO event forces the cache to regenerate artist rows using the new collapsed identities"
      pattern: "buildArtists"
---

<objective>
Deploy the collapse-wired backend (Plan 02-02) and the artwork migration (Plan 02-03) to the Pi
TOGETHER — per D-10, the collapse must never ship without the migration, since it is what re-points
existing artwork onto the new artist identities the collapse creates. This plan is the one place in
the phase that actually touches the live 258 MB artwork directory and the live library.db, so it is
human-reviewed before it writes anything (per the standing orchestrator instruction for this specific
kind of plan) — hence `autonomous: false`.

Purpose: prove all 6 ARTIST-*/ART-* requirements and all 5 ROADMAP Phase 2 success criteria live, with
real before/after evidence (D-11) — not just green unit tests.
Output: a DB backup, a live-verified deploy, a checked-in `deploy/verify-artist-artwork-migration.sh`
PASS/FAIL gate script (reusable for future regression checks), and dated evidence files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-artist-identity-artwork-migration/02-CONTEXT.md
@.planning/phases/02-artist-identity-artwork-migration/02-ARTIST-CORPUS.md
@deploy/verify-data-integrity.sh
@deploy/rebuild-cache.py
@internal/infra/cache/artistmigration.go
@internal/infra/cache/albummigration.go
</context>

<interfaces>
<!-- Live baseline captured 2026-08-12, during planning, on the real Pi — re-verify these numbers
     live in Task 1 rather than trusting them stale, but use them as the expected ballpark. -->

Live baseline (measured 2026-08-12, pre-Phase-2 deploy):
- `sqlite3 ~/stellar-backend/data/library.db "SELECT COUNT(*) FROM albums;"` → 81
- `sqlite3 ... "SELECT COUNT(*) FROM albums WHERE artwork_id IS NULL OR artwork_id='';"` → 40
- `sqlite3 ... "SELECT COUNT(*) FROM artwork WHERE type='album' AND album_id NOT IN (SELECT id FROM albums);"` → 38
- `sqlite3 ... "SELECT COUNT(*) FROM artists;"` → 49 (pre-collapse; will shrink after this deploy)
- Backend paths (from CLAUDE.md): DB at `~/stellar-backend/data/library.db`, artwork dir at
  `~/stellar-backend/data/cache/artwork` (258 MB), service `stellar-backend` (`Type=notify`,
  `WatchdogSec=30`), deploy = `make build` → `scp bin/stellar-arm64` to `~/stellar-backend/stellar`
  → `sudo systemctl restart stellar-backend`.
- Pi SSH: per this prompt's `<environment>` block — source the workspace-root `.env`, use
  `sshpass -f <(printf '%s' "$RASPBERRY_PI_SSH_PASSWORD") ssh eduardo@stellar.local '<cmd>'`. Never
  put the password in argv/echo/SSHPASS.

From deploy/rebuild-cache.py (existing, reusable as-is): a python-socketio client that emits
`library:cache:rebuild` and blocks until `pushLibraryCacheStatus`'s `lastUpdated` advances,
confirming a rebuild actually completed (not just that `isBuilding` flickered).
Usage: `python3 deploy/rebuild-cache.py [URL] [TIMEOUT_SECONDS]` — exit 0 on confirmed completion.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Backup, before-evidence, and human-assisted album-orphan matching</name>
  <files>none (investigation + evidence capture; no source files written in this task)</files>
  <read_first>
    internal/infra/cache/albummigration.go (ListOrphanedAlbumArtwork's exact query, to reproduce with
    raw sqlite3 against the CURRENT pre-deploy binary+DB)
    .planning/phases/02-artist-identity-artwork-migration/02-ARTIST-CORPUS.md
  </read_first>
  <action>
    On the Pi (SSH per this plan's `<interfaces>` block): (1) take a timestamped backup —
    `sqlite3 ~/stellar-backend/data/library.db ".backup ~/stellar-backend/data/library.db.pre-phase2-<UTC-timestamp>.bak"`
    — then confirm `sqlite3 <backup-path> "PRAGMA integrity_check;"` returns exactly `ok`. (2) Capture
    BEFORE evidence into `.planning/phases/02-artist-identity-artwork-migration/evidence/before-<timestamp>.txt`:
    artist count, artist-artwork-linked count, albums-without-artwork count, orphaned
    album-artwork count (the four queries from this plan's `<interfaces>` block, re-run live rather
    than trusted from the stale baseline), plus the full raw `sqlite3 ... "SELECT name FROM artists
    ORDER BY name;"` list (this is the pre-collapse list Plan 02-01/02-02 will change). (3) List the
    orphaned album-artwork rows (id, album_id, file_path) and the albums missing artwork (id, title,
    album_artist, source) via raw sqlite3 SELECTs — do not deploy the new binary yet; this step
    inspects the CURRENT (pre-deploy) live state only. (4) For each orphaned artwork row (up to 38),
    `scp` the JPG from `~/stellar-backend/data/cache/artwork/albums/<hash>.jpg` to the local
    scratchpad and view it with the Read tool; cross-reference against the list of albums missing
    artwork captured in step 3 (title + album_artist + source). Propose a mapping ONLY for pairs that
    are visually identifiable with high confidence (e.g. a distinctive, recognizable album cover
    matching a specific title in the missing-artwork list, or matching an artist whose other albums
    are visually consistent). Do not force-match ambiguous, generic, or low-resolution covers — per
    the ROADMAP's own "toward 0" success bar (not "to 0"), leaving low-confidence orphans unmatched is
    the correct, honest outcome, not a shortfall.
  </action>
  <verify>
    <automated>MISSING — this task's proof is a live SSH investigation, not a repo-local automated command; Task 2's `deploy/verify-artist-artwork-migration.sh` (written in this plan) is the automated gate for the outcome this task feeds.</automated>
  </verify>
  <done>Backup taken and integrity-checked on the Pi; before-evidence file written; a proposed orphan-to-album mapping (possibly empty) exists with a justification per pair.</done>
  <acceptance_criteria>
    A backup file exists on the Pi and its `PRAGMA integrity_check` reports `ok`. A before-evidence
    file exists under `.planning/phases/02-artist-identity-artwork-migration/evidence/` containing
    real command output (not summarized/paraphrased numbers). A written proposed-mapping list exists
    (orphan artwork id → target album id, with a one-line visual justification per pair) — the list
    may legitimately be empty if no confident visual match exists for any of the 38.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the proposed album-artwork matches before anything is written</name>
  <what-built>
    A DB backup was taken and verified on the Pi. Before-evidence (artist list, artwork counts) was
    captured. Zero writes have happened yet. N of up to 38 orphaned album-artwork images were visually
    inspected and matched against currently-unlinked albums with a stated justification per match (N
    may be 0).
  </what-built>
  <how-to-verify>
    Review the proposed orphan→album mapping list from Task 1 (each entry: orphan artwork filename,
    proposed target album title + artist, and the one-line visual justification). Confirm each
    proposed pair looks right, or reject specific ones. No DB write or Pi deploy happens until you
    respond.
  </how-to-verify>
  <resume-signal>
    "approved" (apply all proposed pairs) / "approved except &lt;list&gt;" (apply all but those) /
    "reject all" (proceed with zero album-artwork rekeys, artist migration only) / free-form
    corrections
  </resume-signal>
  <files>none (approval gate only)</files>
  <action>Present the Task 1 proposed orphan-to-album mapping (with justification per pair) to the
  user per &lt;how-to-verify&gt; above; do not proceed to Task 3's writes until the resume-signal is
  received.</action>
  <verify><human-check>User has typed "approved" (in full, partial, or "reject all" form) in response to this checkpoint.</human-check></verify>
  <done>User has explicitly approved (fully, partially, or rejected) the proposed album-artwork matches before any DB write happens.</done>
</task>

<task type="auto">
  <name>Task 3: Apply, deploy, rebuild, and live-verify</name>
  <files>deploy/verify-artist-artwork-migration.sh</files>
  <read_first>
    deploy/verify-data-integrity.sh (the PASS/FAIL gate idiom to match exactly — `check()` helper,
    `set -uo pipefail`, non-zero exit on any FAIL, env-var overrides with sane defaults)
    deploy/rebuild-cache.py
  </read_first>
  <action>
    (1) For each pair confirmed in the Task 2 checkpoint (if any), apply it via direct parameterized
    `sqlite3` statements mirroring EXACTLY the two-statement transaction in
    `internal/infra/cache/albummigration.go`'s `RekeyAlbumArtwork` (rename the artwork row's `id`/
    `album_id` to the target album's id + `"_artwork"`, then set `albums.artwork_id`) — the Go binary
    with that exact logic is not deployed yet at this point, so apply it by hand with the identical
    SQL so behavior is provably the same code path future boots will run. (2) `make build` (ARM64),
    `scp bin/stellar-arm64` to `~/stellar-backend/stellar`, `sudo systemctl restart stellar-backend`
    — this single deploy carries BOTH the collapse-wired builder (Plan 02-02) and the auto-run artist
    migration hook (Plan 02-03's `InitializeCache` wiring), satisfying D-10's "never separately" rule
    by construction (this plan depends_on both 02-02 and 02-03). (3) Confirm `curl -fsS
    localhost:3000/health` and `curl -fsS localhost:3000/ready` both return 200. (4) Trigger and await
    a rebuild: `python3 deploy/rebuild-cache.py` (forces `buildArtists()` to re-run with the
    collapse-wired code, replacing the pre-collapse artist rows). (5) Capture AFTER evidence (the same
    four queries as Task 1 step 2) into
    `.planning/phases/02-artist-identity-artwork-migration/evidence/after-<timestamp>.txt`, and diff
    against the before-evidence file. (6) Write `deploy/verify-artist-artwork-migration.sh` (new file,
    matching `deploy/verify-data-integrity.sh`'s `check()`/PASS/FAIL/non-zero-exit idiom exactly),
    with gates: (a) exactly one artist row named exactly `Herbert von Karajan` and zero rows `LIKE
    '%Wiener Philharmoniker%'`; (b) exactly one artist row named exactly `Luciano Pavarotti` and zero
    rows `LIKE 'Luciano Pavarotti,%'`; (c) zero artist rows with an empty name; (d) artist-artwork-linked
    count after >= before (D-11: no regression); (e) albums-without-artwork count after <= before
    (and strictly less if any Task 1/2 matches were confirmed and applied); (f) idempotence — touch a
    marker file, restart the service a second time (or re-run `deploy/rebuild-cache.py`), re-check
    gates (a)-(e) are unchanged, and confirm `find ~/stellar-backend/data/cache/artwork -newer
    <marker>` returns zero files (no artwork file was created, modified, or touched by the second
    run). (7) Run the script on the Pi; every gate must PASS. (8) Live user-visible check: write and
    run a short python-socketio client (same connect/emit/on pattern as `deploy/rebuild-cache.py`)
    that emits `library:artists:list` with payload
    `{"scope":"all","sort":"alphabetical","limit":200,"offset":0}` and listens for
    `pushLibraryArtists`; grep the received JSON for exactly one `"name":"Herbert von Karajan"`,
    exactly one `"name":"Luciano Pavarotti"`, and zero `"name":""` — this is what actually renders on
    the LCD's Library → Artists screen.
  </action>
  <verify>
    <automated>bash deploy/verify-artist-artwork-migration.sh</automated>
  </verify>
  <done>The Pi is running the collapse-wired + migration-wired binary; deploy/verify-artist-artwork-migration.sh passes every gate including its idempotence re-run; the live artist-list fetch matches the ROADMAP acceptance language exactly.</done>
  <acceptance_criteria>
    Script exits 0 on the live Pi (all gates PASS, including the idempotence re-run). The
    python-socketio artist-list fetch shows "Herbert von Karajan" exactly once, "Luciano Pavarotti"
    exactly once, zero empty-name entries. Albums-without-artwork count in the after-evidence file is
    less than or equal to the before-evidence file's count.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSH-driven production deploy + live DB mutation | This plan is the only one in the phase that writes to the live appliance's DB and artwork directory. |
| Human-confirmed album match → DB write | The one place in this phase where a human judgment call (not a deterministic algorithm) drives a data write. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | Human-confirmed album-artwork rekey | mitigate | DB backup taken and integrity-checked BEFORE any write (Task 1); the checkpoint (Task 2) requires explicit human approval before any write happens; the rekey is a rename-in-place (reversible via the backup), never a delete. |
| T-02-04-02 | Repudiation | Live migration outcome | mitigate | Before/after evidence files are written to `.planning/phases/.../evidence/` with real command output, not summarized claims (D-11). |
| T-02-04-03 | Denial of Service | `systemctl restart` during deploy | accept | Same restart risk profile as every existing Phase 1 deploy step; `/health` and `/ready` checks gate the rest of the task, and the systemd watchdog (`WatchdogSec=30`) already covers hang detection. |
| T-02-04-SC | Supply chain | `python3 deploy/rebuild-cache.py` dependency | mitigate | `python-socketio` is an existing, already-installed dependency from Phase 1 (verify with `pip3 show python-socketio` before relying on it, per `deploy/verify-data-integrity.sh`'s existing precedent) — no new package installs in this plan. |
</threat_model>

<verification>
`bash deploy/verify-artist-artwork-migration.sh` exits 0 on the live Pi, including its idempotence
re-run gate.
`curl -fsS localhost:3000/health` and `/ready` both 200 after the deploy.
Live `library:artists:list` fetch (via python-socketio) matches the ROADMAP's exact acceptance
language: one Karajan row, one Pavarotti row, zero blank rows.
</verification>

<success_criteria>
All 5 ROADMAP Phase 2 success criteria are true on the real Pi, proven with command output and/or a
live Socket.IO fetch — not asserted. The collapse and the artwork migration deployed together, in one
binary, per D-10. `deploy/verify-artist-artwork-migration.sh` exists as a reusable regression gate for
future phases that touch artist/artwork identity.
</success_criteria>

<output>
Create `.planning/phases/02-artist-identity-artwork-migration/02-04-SUMMARY.md` when done
</output>
