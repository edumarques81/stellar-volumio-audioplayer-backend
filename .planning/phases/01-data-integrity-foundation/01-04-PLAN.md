---
phase: 01-data-integrity-foundation
plan: 04
type: execute
wave: 2
depends_on: ["01-03"]
files_modified:
  - deploy/verify-data-integrity.sh
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "mpc stats on the Pi reports a song count matching the real (non-._) file count on /mnt/ssd/Music, with zero ._-prefixed entries remaining in MPD's index -- verified live via SSH (ROADMAP Phase 1 success criterion 1)."
    - "/mnt/ssd is restored to its original read-only mount state after the deletion, preserving the deliberate safety posture recorded in /etc/fstab."
    - "Only the exact files reviewed and approved in Plan 01-03's checkpoint were deleted -- nothing outside /mnt/ssd/Music, nothing not matching the ._ prefix."
  artifacts:
    - path: "deploy/verify-data-integrity.sh"
      provides: "A reusable PASS/FAIL gate script (matching deploy/verify-cutover.sh's idiom) asserting mpc stats song count == real file count and zero ._ entries in MPD's index"
      contains: "mpc stats"
  key_links:
    - from: "deploy/verify-data-integrity.sh"
      to: "mpc listall"
      via: "grep -c for any remaining basename starting with ._"
      pattern: "\\._"
---

<objective>
Delete exactly the `._` files enumerated and approved in Plan 01-03's manifest, restore the SSD's
read-only mount, rescan MPD, and verify ROADMAP Phase 1 success criterion 1 (`mpc stats` song count
matches the real file count, zero `._` entries remain). Adds a reusable verification script so this
check can be re-run at any time in the future, not just during this plan.

Purpose: closes out DATA-03. This is the one plan in the phase that performs an irreversible action
(file deletion) against `/mnt/ssd/Music`, gated entirely on Plan 01-03's already-obtained user
approval -- no new checkpoint is added here (D-03's "confirm, then delete" was satisfied by 01-03;
this plan IS the "then delete, then mpc update and verify" half).

Output: `/mnt/ssd/Music` free of `._` junk, MPD's index matching reality, and
`deploy/verify-data-integrity.sh` as a durable, reusable PASS/FAIL check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-integrity-foundation/01-CONTEXT.md
@deploy/verify-cutover.sh
</context>

<interfaces>
From deploy/verify-cutover.sh:1-22 (the PASS/FAIL gate idiom to mirror -- `check()` helper,
`FAILED` counter, `set -uo pipefail`, section-header echo lines):

  #!/bin/bash
  set -uo pipefail
  BASE="${BASE:-http://localhost:3000}"
  FAILED=0
  check() {
    local name="$1" status="$2"
    if [ "$status" = "PASS" ]; then echo "  OK $name"; else echo "  FAIL $name"; FAILED=$((FAILED + 1)); fi
  }
  echo "=== Backend service (Pi-hosted) ==="
  if is_active stellar-backend.service; then check "S1 stellar-backend active" PASS; else check "S1 stellar-backend active" FAIL; fi
  ...
  exit $FAILED   # (or similar, see the tail of the file)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Delete manifest files, remount read-only, rescan</name>
  <files>none (Pi-side action)</files>
  <read_first>
    .planning/phases/01-data-integrity-foundation/01-03-SUMMARY.md (confirms the manifest path and
    the user's approval)
  </read_first>
  <action>
    SSH to the Pi per the workspace credential convention (source `.env`, `sshpass -f <(printf ...)`,
    never argv/echo the password). On the Pi, in order:
    1. `sudo mount -o remount,rw /mnt/ssd` (the SSD's block device is not hardware write-protected --
       verified via `blockdev --getro /dev/sda1` returning 0 during planning -- this is a software
       mount-option flip only).
    2. Delete ONLY the paths recorded in `~/stellar-backend/data/dotunderscore-manifest.txt`, read
       back via `xargs -0`, with a defense-in-depth re-check per entry immediately before removal:
       the path's basename must start with `._` AND the path must be under `/mnt/ssd/Music` -- skip
       and log (do not abort) any manifest line failing either check, so a stale or tampered manifest
       entry cannot cause an out-of-scope deletion. Use `rm -f -- "$path"` (the `--` guards against a
       path that happens to start with `-`).
    3. `sudo mount -o remount,ro /mnt/ssd` (restore the original fstab-declared posture regardless of
       whether step 2 fully succeeded -- run this even on partial failure, e.g. via a shell `trap` or
       explicit run in both the success and failure branches).
    4. `mpc update` then poll (`mpc status` or `mpc idle database` with a timeout, e.g. 60s) until the
       database update finishes, matching MPD's async-update semantics.
  </action>
  <verify>
    <automated>cd /Users/eduardomarques/workspace/stellar-streamer && set -a && . ./.env && set +a && sshpass -f <(printf '%s' "$RASPBERRY_PI_SSH_PASSWORD") ssh eduardo@stellar.local "mount | grep -q 'on /mnt/ssd type exfat (ro' && echo REMOUNT_RO_OK"</automated>
  </verify>
  <done>All manifest-listed files removed from /mnt/ssd/Music; /mnt/ssd is back to `ro` in `mount`
  output; MPD has completed a database update since the deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Add deploy/verify-data-integrity.sh and confirm criterion 1</name>
  <files>deploy/verify-data-integrity.sh</files>
  <read_first>
    deploy/verify-cutover.sh (full file, for the check()/FAILED/exit-code idiom to mirror)
  </read_first>
  <action>
    Create `deploy/verify-data-integrity.sh`, runnable on the Pi (usage comment:
    `bash ~/stellar-backend/deploy/verify-data-integrity.sh`), following verify-cutover.sh's exact
    style (`set -uo pipefail`, a `check()` helper incrementing a `FAILED` counter, section-header
    echo lines, non-zero exit on any failure). Implement two gates:
    - `I1`: real audio file count on `/mnt/ssd/Music` (via `find`, restricted to the audio
      extensions flac/wav/dsf/dff/mp3/m4a/ape/wv/aiff/alac, excluding `._`-prefixed basenames) equals
      the `Songs:` line from `mpc stats`.
    - `I2`: `mpc listall | grep -c '/\._'` (matching a `._`-prefixed basename anywhere in an entry's
      path, filtered with `grep -v '^#'` -- not applicable here since `mpc listall` has no comment
      lines, but keep the pattern explicit and anchored to `/\._` so it cannot false-positive on a
      real file that merely contains the substring `._` mid-name) reports `0`.
    Run the script on the Pi via SSH and capture its output; both gates must PASS.
  </action>
  <verify>
    <automated>cd /Users/eduardomarques/workspace/stellar-streamer && set -a && . ./.env && set +a && sshpass -f <(printf '%s' "$RASPBERRY_PI_SSH_PASSWORD") ssh eduardo@stellar.local 'bash ~/stellar-backend/deploy/verify-data-integrity.sh; echo "EXIT:$?"'</automated>
  </verify>
  <done>deploy/verify-data-integrity.sh exists in the repo, has been copied to and run on the Pi, and
  both I1 (song count matches real file count) and I2 (zero ._ entries in MPD's index) PASS --
  satisfying ROADMAP Phase 1 success criterion 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent SSH session -> Pi filesystem (write) | The only destructive action in this phase: deleting files under a normally-read-only music mount. |
| Mount state | Transiently rw during deletion; must reliably return to the fstab-declared ro state even on partial failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Tampering | delete loop | mitigate | Re-validates each manifest entry's `._` prefix and `/mnt/ssd/Music` root immediately before removal (defense in depth against a stale/edited manifest between enumerate and delete). |
| T-01-11 | Denial of Service | remount rw window | mitigate | Remount-to-ro runs unconditionally after the delete loop (success or failure path), minimizing the time the mount is writable and guaranteeing the original safety posture is restored. |
| T-01-12 | Repudiation | deletion has no undo | accept | D-02 already established `._` files are disposable macOS sidecar junk, not user content; the manifest (Plan 01-03) is the durable record of what was removed and why. |
</threat_model>

<verification>
1. `deploy/verify-data-integrity.sh` exit code 0 when run on the Pi.
2. `mount | grep /mnt/ssd` shows `ro` (mount state restored).
3. `mpc stats` Songs count equals the real (non-`._`) audio file count on `/mnt/ssd/Music`.
</verification>

<success_criteria>
ROADMAP Phase 1 success criterion 1 is met: `mpc stats` song count matches the real file count with
zero `._`-prefixed entries remaining in MPD's index, verified live via SSH.
</success_criteria>

<output>
Create `.planning/phases/01-data-integrity-foundation/01-04-SUMMARY.md` when done
</output>
