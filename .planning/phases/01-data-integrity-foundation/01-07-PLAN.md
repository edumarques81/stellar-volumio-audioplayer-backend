---
phase: 01-data-integrity-foundation
plan: 07
type: execute
wave: 4
depends_on: ["01-05", "01-06"]
files_modified:
  - .planning/phases/01-data-integrity-foundation/DATA-01-RETAG-RECOMMENDATIONS.md
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "After the user's retag (Plan 01-06), the R. Strauss / Karajan-VPO album appears in Library -> Albums with all 10 tracks present and playable, and the toe and Sigxer SU-6 folders likewise appear under their artists (ROADMAP Phase 1 success criterion 2)."
    - "The cache-status SkippedCount reads 0 once the retag is verified (ROADMAP Phase 1 success criterion 3 / D-09)."
  artifacts: []
  key_links:
    - from: "cache_meta.skipped_count (Pi sqlite)"
      to: "ROADMAP Phase 1 success criterion 3"
      via: "direct sqlite3 read post-rebuild, cross-checked against pushLibraryCacheStatus's skippedCount field"
      pattern: "skipped_count"
---

<objective>
Runs independently, any time after Plan 01-06's human-action checkpoint reports the retag is done
(potentially in a separate session/context window) -- per the phase brief's explicit instruction to
structure this verification so it is not tangled up with the out-of-band human step it depends on.
Verifies ROADMAP Phase 1 success criteria 2 and 3 against the live Pi: the 3 previously-untagged
folders now browse correctly, and the skipped-count telemetry from Plan 01-02 reads 0.

Purpose: closes DATA-01 and DATA-02. This is intentionally the LAST plan in the phase -- it is the
only one gated on work outside the agent's control (the user's own tag editor), so every other DATA-0X
concern is fully verified beforehand and this plan has nothing left to do but confirm the human step
landed correctly.

Output: a verification note appended to `DATA-01-RETAG-RECOMMENDATIONS.md` recording the pass/fail
result and the final `skippedCount` value.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-integrity-foundation/01-CONTEXT.md
@.planning/phases/01-data-integrity-foundation/DATA-01-RETAG-RECOMMENDATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rescan, rebuild cache, and verify the 3 folders browse correctly</name>
  <files>none (verification only)</files>
  <read_first>
    .planning/phases/01-data-integrity-foundation/DATA-01-RETAG-RECOMMENDATIONS.md (the exact folder
    list + recommended tag values from Plan 01-06, to know what to check for)
  </read_first>
  <action>
    SSH to the Pi per the workspace credential convention. Run `mpc update` and wait for completion.
    For each of the 3 folders from `DATA-01-RETAG-RECOMMENDATIONS.md`, confirm via `mpc find album
    "<recommended album>"` (or `mpc search albumartist "<recommended albumartist>"`) that MPD now
    returns the expected track count (10 for the Karajan/VPO folder, 4 for `toe`, 2 for the Singxer
    SU-6 folder -- or whatever the live counts from Plan 01-06 established, if they differed from the
    stale baseline). Trigger a backend cache rebuild (`library:cache:rebuild` or the frontend console
    helper) and wait for completion. Query
    `sqlite3 ~/stellar-backend/data/library.db "SELECT title, album_artist, track_count FROM albums
    WHERE title LIKE '%<keyword from each folder>%';"` for each of the 3 albums and confirm each
    appears with the expected track_count (not silently dropped, per the original R1 symptom: "artist
    visible, zero songs"). Also confirm each album's artist appears in the `artists` table with a
    nonzero `album_count` that includes the newly-visible album.
  </action>
  <verify>
    <automated>cd /Users/eduardomarques/workspace/stellar-streamer && set -a && . ./.env && set +a && sshpass -f <(printf '%s' "$RASPBERRY_PI_SSH_PASSWORD") ssh eduardo@stellar.local 'sqlite3 ~/stellar-backend/data/library.db "SELECT COUNT(*) FROM albums WHERE track_count > 0;"'</automated>
  </verify>
  <done>All 3 previously-untagged folders resolve to real albums in MPD (correct track counts) and in
  the backend's rebuilt cache (correct track_count, linked to a real artist with nonzero
  album_count).</done>
</task>

<task type="auto">
  <name>Task 2: Confirm SkippedCount reads 0 and record the result</name>
  <files>.planning/phases/01-data-integrity-foundation/DATA-01-RETAG-RECOMMENDATIONS.md</files>
  <read_first>
    internal/infra/cache/sqlite.go (GetStats, from Plan 01-02 -- confirms the "skipped_count" meta key)
  </read_first>
  <action>
    Query `sqlite3 ~/stellar-backend/data/library.db "SELECT value FROM cache_meta WHERE key =
    'skipped_count';"` on the Pi and confirm the value is `0` (per D-09: success is the count reading
    0 after the retag, not merely that the mechanism exists). Cross-check by also reading the live
    Socket.IO payload if convenient (e.g. via the LCD kiosk's DevTools socket tap pattern:
    `socketService.emit('library:cache:status')` and inspect the next `pushLibraryCacheStatus` frame
    for `skippedCount: 0`) -- the sqlite read is the required automated check; the socket cross-check
    is a nice-to-have sanity confirmation, not a blocking requirement. If the value is NOT 0, do not
    treat this as a plan failure to silently paper over: report the actual value and which folder(s)
    are still untagged (re-run Task 1's per-folder `mpc find` checks to identify which ones), since
    that means the user's retag is incomplete or a new untagged file has appeared. Append a
    "## Verification Result" section to the end of `DATA-01-RETAG-RECOMMENDATIONS.md` recording the
    date, the final skippedCount value, and the per-folder track-count confirmations from Task 1.
  </action>
  <verify>
    <automated>cd /Users/eduardomarques/workspace/stellar-streamer && set -a && . ./.env && set +a && sshpass -f <(printf '%s' "$RASPBERRY_PI_SSH_PASSWORD") ssh eduardo@stellar.local "sqlite3 ~/stellar-backend/data/library.db \"SELECT value FROM cache_meta WHERE key='skipped_count';\""</automated>
  </verify>
  <done>cache_meta.skipped_count reads "0" on the Pi; DATA-01-RETAG-RECOMMENDATIONS.md carries a
  dated Verification Result section recording this and the per-folder confirmations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Read-only verification against the already-deployed, already-hardened backend (Plan 01-02, 01-05) and the user's own completed retag (Plan 01-06). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-16 | Repudiation | "retag complete" human signal | accept | This plan does not trust the human-action resume signal alone -- it independently re-derives track/album counts and the skipped_count meta value from the live system before declaring success, so a premature or inaccurate "done" claim is caught rather than rubber-stamped. |
</threat_model>

<verification>
1. `mpc find album "..."` (or equivalent) returns the expected track count for each of the 3
   previously-untagged folders.
2. `cache_meta.skipped_count` on the Pi reads `0`.
3. `DATA-01-RETAG-RECOMMENDATIONS.md` carries a dated Verification Result section.
</verification>

<success_criteria>
ROADMAP Phase 1 success criteria 2 and 3 are both met live: the 3 folders are findable and playable
under their artists, and the skipped-count telemetry confirms zero untagged real songs remain.
Combined with Plan 01-04 (criterion 1) and Plan 01-05 (criterion 4), all four Phase 1 success
criteria are satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-data-integrity-foundation/01-07-SUMMARY.md` when done
</output>
